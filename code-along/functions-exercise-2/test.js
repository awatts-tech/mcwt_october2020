function() {
    // Code goes in here
    let facingForward = false;
    let position = 0;
    function moveForward(distance) {
        if (facingForward === true) {

        // increase position by distance
        position = position + distance;
    } else {
        position = position - distance;

    }
}

        }
    function moveBackward(distance) {
        // decrease position by distance
    }
}